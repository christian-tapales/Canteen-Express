# ===============================================
# UNIFIED .gitignore (MyWebProject Root)
# Applies rules for both /backend (Spring Boot)
# and /frontend (React/Node) projects.
# ===============================================

# ---------------------------------------------
# 1. SPRING BOOT / JAVA (inside /backend)
# ---------------------------------------------

# Maven Build Output
/backend/target/
/backend/HELP.md
/backend/.mvn/wrapper/maven-wrapper.jar

# NOTE: The exclusions (!) for target are often unnecessary and complex.
# We ignore the main target/ folder completely, which is standard practice.

# STS / Eclipse IDE Files (prefixed with /backend)
/backend/.apt_generated
/backend/.classpath
/backend/.factorypath
/backend/.project
/backend/.settings/
/backend/.springBeans
/backend/.sts4-cache

# IntelliJ IDEA Files (prefixed with /backend)
/backend/.idea/
/backend/*.iws
/backend/*.iml
/backend/*.ipr

# NetBeans Files (prefixed with /backend)
/backend/nbproject/private/
/backend/nbbuild/
/backend/dist/
/backend/nbdist/
/backend/.nb-gradle/
/backend/build/
!/backend/**/src/main/**/build/ # Exclusions originally found
!/backend/**/src/test/**/build/

# ---------------------------------------------
# 2. REACT / NODE (inside /frontend)
# ---------------------------------------------

# Dependency and Build Artifacts (prefixed with /frontend)
/frontend/node_modules/
/frontend/dist/
/frontend/dist-ssr/
/frontend/*.local
/frontend/coverage/

# Logs (applies to both, but commonly generated by Node)
/frontend/logs
/frontend/*.log
/frontend/npm-debug.log*
/frontend/yarn-debug.log*
/frontend/yarn-error.log*
/frontend/pnpm-debug.log*
/frontend/lerna-debug.log*

# ---------------------------------------------
# 3. COMMON FILES (Applies to root and subfolders)
# ---------------------------------------------

# IDE & Editor Files (applies anywhere)
.idea
*.swp
*.suo
*.ntvs*
*.njsproj
*.sln

# VS Code specific rules
# We ignore .vscode globally, but exclude the extensions.json
# if it is needed for consistent development setup.
.vscode/
!.vscode/extensions.json

# OS Files
.DS_Store
Thumbs.db

# Secrets and Environment Variables
.env
.env.*
!*.example.env